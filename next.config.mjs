/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['res.cloudinary.com'], // Add this line to allow images from res.cloudinary.com
    },
  };
  
  export default nextConfig;
  
